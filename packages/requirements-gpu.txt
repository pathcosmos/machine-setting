# GPU-specific packages (NVIDIA CUDA)
# Install with: uv pip install -r requirements-gpu.txt --index-url <cuda-url> --extra-index-url https://pypi.org/simple
#
# Note: nvidia-* runtime libraries are NOT listed here intentionally.
# They are auto-resolved as torch dependencies. Pinning them breaks
# cross-CUDA-version compatibility (e.g. cu130 index may lack specific nvidia-* versions).

# PyTorch ecosystem (GPU builds — version resolved by index)
torch
torchaudio
torchvision
triton

# CUDA Python bindings (from PyPI)
cuda-bindings
cuda-pathfinder

# GPU-accelerated ML (from PyPI via --extra-index-url)
# flash-attn  # optional: skip if no prebuilt wheel for your CUDA (e.g. cu130); install manually when available
bitsandbytes
onnxruntime-gpu

# Distributed training & optimization
deepspeed
pytorch-lightning
torchmetrics

# GPU monitoring (Python)
pynvml
gpustat
nvitop

# Inference optimization
vllm
optimum
