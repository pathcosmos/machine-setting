# Profile: GPU Workstation
# Full AI/ML environment with GPU acceleration

PACKAGE_GROUPS="core data web"
GPU_PACKAGES=true
INSTALL_NODE=true
INSTALL_JAVA=true

# NVIDIA: full PC-grade stack (driver + CUDA + cuDNN + NCCL + tools)
INSTALL_NVIDIA=true
NVIDIA_ENTERPRISE=false
NVIDIA_SYSTEM_TOOLS=true
NVIDIA_KERNEL_TUNING=true
